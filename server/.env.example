NODE_ENV=development
PORT=3000
# Matches the docker-compose Postgres service (host port 5433 to avoid
# clashing with CloudOps Lab's Postgres on 5432)
DATABASE_URL=postgresql://devboard:devboard@localhost:5433/devboard
# Phase 3 — fine-grained GitHub token (read-only) goes here later
# GITHUB_TOKEN=
