NODE_ENV=development
PORT=3000
# Matches the docker-compose Postgres service on host port 5433.
DATABASE_URL=postgresql://devboard:devboard@localhost:5433/devboard
# Fine-grained permissions: Metadata, Contents, Issues, and Pull requests (read-only).
GITHUB_TOKEN=
GITHUB_CACHE_TTL_SECONDS=300
